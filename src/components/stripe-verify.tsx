import { Button, Link } from "@payloadcms/ui";

export const StripeVerify = () => {
    return (
        <div>
            <Link href="/stripe-verify">
                <Button>Stripe Verification account</Button>
            </Link>
        </div>
    );
};
